use TestSupport

title is "% Operator"
section is "declaration.operators.integer"
topic is "% Operator on 2 integers should return reminder of devision first operand by second" 

return 4 % 2 = 0 and 5 % 2 = 1
