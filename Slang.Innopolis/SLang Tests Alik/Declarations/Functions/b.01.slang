use TestSupport

title is "Declare invalid procedure with default parameters"
section is "declaration.functions-procedures"
topic is "All parameters followed by first default parameters should be have default value" 

// Assert
func(a is 4, b is Integer): Integer is
	none
end func