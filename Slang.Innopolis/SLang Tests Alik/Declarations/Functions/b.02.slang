use TestSupport

title is "Declaring function without return value"
section is "declaration.functions-procedures"
topic is "Functions should have return value" 

// Assert
mult(a, b: Integer): Integer is
	none
end mult
