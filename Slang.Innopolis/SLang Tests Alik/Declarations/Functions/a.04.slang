use TestSupport

title is "Declare empty procedure"
section is "declaration.functions-procedures"
topic is "Simple empty procedure declaration that should be valid for compiler" 

// Assert
func() is
	none
end