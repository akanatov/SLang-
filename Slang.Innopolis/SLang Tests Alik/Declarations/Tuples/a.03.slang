use TestSupport  

title is "getting tuple element by name"
section is "declarations.tuples" 
topic is "get tuple element by key"


tuple is (num: 1, name: "str", flag: true)

return if tuple.flag then 1 else 0 end