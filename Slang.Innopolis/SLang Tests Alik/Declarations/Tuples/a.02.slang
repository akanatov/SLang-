use TestSupport  

title is "tuples with named values"
section is "declarations.tuples" 
topic is "declared tuple with field names should compile"


tuple is (num: 1, name: "str", flag: true)
tup is (num: 1, name: "str", false)