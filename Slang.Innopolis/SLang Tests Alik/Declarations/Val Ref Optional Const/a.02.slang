use TestSupport

title is "Declare value-type variable"
section is "declaration.(var-ref-optionals-const)"
topic is "Types declared with 'val' modifier should be valid for compiler" 

// Assert
b : val Integer
