use TestSupport

title is "Declare nullable variable"
section is "declaration.(var-ref-optionals-const)"
topic is "Types declared with '?' modifier should be valid for compiler" 

// Assert
b : ?Integer