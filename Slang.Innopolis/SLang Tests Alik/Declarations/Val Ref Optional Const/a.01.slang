use TestSupport

title is "Declare reference variable"
section is "declaration.(var-ref-optionals-const)"
topic is "Types declared with 'ref' modifier should be valid for compiler" 

// Assert
b : ref Integer