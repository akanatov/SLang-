use TestSupport

title is "Assigning value to const variables"
section is "declaration.(var-ref-optionals-const)"
topic is "Variables declared with const modifier can not be modified"

// Arrange
const b is Integer

// Act
b = 1