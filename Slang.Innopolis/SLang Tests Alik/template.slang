use TestSupport  // optional

title is "short test description"
section is "declaration.variables" // “номер раздела определения языка”
topic is "expanded description testing properties" //”развернутое описание проверяемого свойства”
// Контейнеры, подпрограммы
// и прочие необходимые объявления
return /* return 1 if ok else 0 */ // выражение
