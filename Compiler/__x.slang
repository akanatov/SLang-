//x(y) := if a or else b do x else y
//result (pos) := if this(pos) = 0 do 0 else 1

//x := x.y() = 0 and then y * r / k ++ z
//x := this(pos) = 0 or else other(pos) = 0

x := if this(pos) = 0 or else other(pos) = 0 do 0 else 1
